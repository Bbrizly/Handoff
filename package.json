{
  "name": "hn",
  "version": "0.1.9",
  "private": true,
  "description": "Local-first mapped remote terminals and execution over SSH with Mutagen",
  "type": "module",
  "bin": {
    "hn": "./src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node src/index.js --help && node src/index.js",
    "test": "node --test",
    "test:integration": "node --test test/integration/*.test.js"
  },
  "license": "MIT"
}
