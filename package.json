{
  "name": "hn",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first remote execution over SSH. Your files and Git stay local, your compute does not.",
  "type": "module",
  "bin": {
    "hn": "./src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node src/index.js --help && node src/index.js",
    "test": "node --test",
    "test:integration": "node --test test/integration/*.test.js"
  },
  "license": "MIT"
}
